// DEPRECATED: single-file Dynamo helper. Use `services/dynamoService.ts` instead.
// Keep a harmless shim to avoid breaking older imports during migration.

/* eslint-disable @typescript-eslint/no-empty-function */
export function __deprecatedDynamoShim() {}

export {};
